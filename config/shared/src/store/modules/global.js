const state = {
  global: []
}

const getters = {
  global: (state) => {
    return state.global
  },
}

const mutations = {
}

const actions = {
}

export default {
  namespaced: true,
  state,
  getters,
  mutations,
  actions
}