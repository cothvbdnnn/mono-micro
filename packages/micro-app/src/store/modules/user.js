const state = {
  user: []
}

const getters = {
  user: (state) => {
    return state.user
  },
}

const mutations = {
}

const actions = {
}

export default {
  namespaced: true,
  state,
  getters,
  mutations,
  actions
}